#include <bits/stdc++.h>
using namespace std;

#define ll long long

int main() {
    ll T;
    cin >> T;
    while (T--) {
        ll N, M;
        cin >> N >> M;
        ll votes_needed = (M/2+ 1);
        cout << votes_needed << endl;
    }
    return 0;
}
